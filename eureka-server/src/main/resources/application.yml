spring:
  application:
    name: eureka-server
  security:
    basic:
      enabled: true
    user:
      name: eureka-admin
      password: NatUALRogrES

server:
  port: 8761

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false